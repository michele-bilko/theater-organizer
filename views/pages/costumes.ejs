<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'), {variant: 'compact'}; %>
</head>
<body class="container">

<header>
  <%- include('../partials/header'); %>
</header>

<main>
    <div class="jumbotron">
      <h1 class= "display-4">Costumes</h1>
      <hr class="my-4">
      <!-- <p>C</p> -->



      <!-- <ul>
        <% rows.forEach(function(row) { %>
          <li>
            <strong><%= row.costumeName %></strong>
             <%= row.isRented %>
             <%= row.isRentable %>
             <%= row.costumeImage %>
             <p><a href ="/rent">Rent</a></p>
          </li>
        <% }); %>
      </ul> -->

      <div class="card-deck">
        <div class="card-columns">  
        <% rows.forEach(function(row) { %>
        <!-- <div class="col-sm-4"> -->
            <div class="card border-dark mb-3" style="width: 20rem;">
                <img class="card-img-top" src="<%= row.costumeImage %>" alt="<%= row.costumeName %> image">
                    <div class="card-body">
                        <h5 class="card-title"><%= row.costumeName %> </h5>
                          <div id = "rentableGreen" hidden>
                            <h5><span class="badge badge-success">Rentable</span>
                          </div>
                          <div id = "rentableRed" hidden>
                              <h5><span class="badge badge-danger">Rentable</span>
                          </div>

                          <div id = "rentedGreen" hidden>
                              <span class="badge badge-success" hidden>Rented</span></h5>
                          </div>
                          <div id = "rentedRed" hidden>
                            <span class="badge badge-danger" hidden>Rented</span></h5>
                          </div>

                          <script>
                            const rentableGreen=document.getElementById('rentableGreen');
                            const rentableRed=document.getElementById('rentableRed');
                            const rentedGreen=document.getElementById('rentedGreen');
                            const rentedRed=document.getElementById('rentedRed');
                            const rentability = row.isRentable;
                            const rentState = row.isRented;
                            console.log(rentability);
                            if(rentability.equals('y')){
                              rentableGreen.hidden=false;
                            }
                            else{
                              rentableRed.hidden=false;
                            }
                            if(rentState.equals('y')){
                              rentedGreen.hidden=false;
                            }
                            else{
                              rentedRed.hidden=false;
                            }
                          </script>

                          <!-- Have badges change based on isRented and isRentable -->
                        <p class="card-text">
                          <%= row.costumeDescription%> 
                          <!-- <%= row.isRented %> <%= row.isRentable %> -->
                        </p>
                        <a href="/rent" class="btn btn-primary">Rent</a>
                    </div>
                    <div class="card-footer">
                        <small class="text-muted">Some info</small>
                    </div>
            </div>
        <!-- </div> -->
        <!-- https://drive.google.com/uc?id=13Yi-mHzbBju24flO6CIWsGz7a9dIFIfM -->
        <% }); %>
        </div>
    </div>

    </div>
  </main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>



      